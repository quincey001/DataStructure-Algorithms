package data_structure.Array;

public class ArrayDemo {
    /**
     * what is an array?
     * An array is the simplest data structure where a collection have a fixed size with the same type of
     * data elements. and each element can be accessed by using the index number.
     * Advantage:
     * 1.random access
     * 2. esay sorting and iteration
     * 3. replacement of multiple variables
     *
     * Disadvantages:
     * 1.fixed size
     * 2. if capacity is bigger than the occupancy, rest of them are wasted
     * 3. needs contiguous memory to get allocated.
     */
}
